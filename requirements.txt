fastapi==0.111.0
uvicorn==0.29.0
sqlalchemy==2.0.30
# Alembic для версионируемых миграций. Подключён параллельно с
# LIGHTWEIGHT_MIGRATIONS (см. alembic/README.md). Используется для
# сложных изменений схемы (DROP COLUMN, data migrations, FK changes).
alembic==1.13.2
# CVE-2026-28684 fixed in 1.2.2
python-dotenv==1.2.2
openai==1.30.1
anthropic==0.26.0
httpx==0.27.0
passlib[bcrypt]==1.7.4
# bcrypt 5.x несовместим с passlib 1.7.4 — passlib делает detect_wrap_bug
# с >72-byte паролем, а новый bcrypt отказывает (ValueError). Pin до 4.x
# до апгрейда passlib.
bcrypt<5.0
# PYSEC-2024-232 / 233 (algo-confusion / DoS) fixed in 3.4.0
python-jose[cryptography]==3.4.0
yookassa==2.3.0
# CVE-2024-53981 / 2026-24486 / 2026-40347 / 2026-42561 — DoS на multipart parsing
python-multipart>=0.0.27
defusedxml==0.7.1
bleach==6.1.0
# PPTX-генерация (для модуля презентаций)
python-pptx==0.6.23
# DOCX-экспорт результатов бизнес-решений
python-docx==1.1.2
# Web Push API (VAPID) для браузерных уведомлений
pywebpush==2.0.0
# PDF-отчёты для бизнес-решений
# CVE-2025-69534 — XSS в markdown.fenced_code, fix в 3.8.1
markdown==3.8.1
# CVE-2024-25885 в xhtml2pdf 0.2.16 не имеет fix version от мейнтейнера —
# отслеживаем upstream. Используется только server-side для PDF-генерации
# из доверенного markdown (наши промпты), не от юзер-input → low risk.
xhtml2pdf==0.2.16
# QR-генерация (для QR-логина и встраивания в КП/презентации)
qrcode==8.2
# 2FA (TOTP) для админки — генерация secret + проверка 6-значных кодов из
# Google Authenticator / Authy / 1Password.
pyotp==2.9.0
# RAG: извлечение текста из XLSX (knowledge base)
openpyxl==3.1.5
# Извлечение текста из PDF.
# CVE-2026-40260/41168/41312/41313/41314 в pypdf <6.10.2 (DoS, infinite loop
# через malicious PDF). pypdf приходит транзитивно от xhtml2pdf — пиним минимум.
PyPDF2==3.0.1
pypdf>=6.10.2

# ── Security constraints на транзитивные зависимости ────────────────────────
# Эти пакеты приходят транзитивно (от requests, httpx, fastapi, alembic),
# pip-audit нашёл в них CVE. Прибиваем минимумы здесь чтобы pip не выбрал
# уязвимую версию при свежей установке. Не трогаем major-bumps (starlette
# требует FastAPI upgrade — отдельная задача).

# CVE-2026-... в cryptography <46.0.7
cryptography>=46.0.7
# CVE-2026-45409 в idna <3.15
idna>=3.15
# PYSEC-2026-141/142 в urllib3 <2.7.0
urllib3>=2.7.0

# KNOWN ISSUE: starlette 0.37.2 имеет CVE-2024-47874 / CVE-2025-54121 /
# PYSEC-2026-161 — fix в 0.40+/1.0.1. Pinned транзитивно через fastapi==0.111
# (требует starlette<0.38). Апгрейд = FastAPI bump до 0.115+, отдельная
# задача с тестированием совместимости.
# Опциональный мониторинг ошибок: ставится только если SENTRY_DSN задан
# (init guarded в main.py); без DSN импорт не происходит, но колесо в env есть.
sentry-sdk[fastapi]==2.15.0
# PostgreSQL driver — опциональный: используется когда DATABASE_URL начинается
# с `postgresql://`. По умолчанию проект работает на SQLite (для dev).
# Для прода ставится через `pip install -r requirements.txt` на сервере.
psycopg2-binary==2.9.9
# Yandex Object Storage (S3-compatible) — для офлайн-бэкапов БД (152-ФЗ ст. 18:
# копия в РФ-облаке). Используется только если YC_S3_BUCKET задан в env.
# boto3 — стандартный AWS SDK, поддерживает S3-совместимое API через endpoint_url.
boto3==1.35.0

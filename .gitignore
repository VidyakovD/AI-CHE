*.db
chat.db.*
backups/
*.pyc
__pycache__/
.env
venv/
.venv/
uploads/
server/.jwt_secret
.backup_encryption_key
*.log

# Frontend build (Tailwind)
node_modules/
# package-lock.json коммитим — гарантирует reproducible build на проде
# views/styles.css генерируется через `npm run build:css` и коммитится в репо

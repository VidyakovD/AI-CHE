name: Deploy to server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Call deploy endpoint
        run: |
          curl -s -X POST "https://aiche.ru/internal/deploy" -H "Authorization: Bearer ${{ secrets.DEPLOY_TOKEN }}"

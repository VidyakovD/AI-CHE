{
  "name": "aiche-frontend-build",
  "version": "1.0.0",
  "private": true,
  "description": "Tailwind CSS build для AI Студия Че. Запускается ТОЛЬКО при изменении классов/HTML, не для каждого деплоя.",
  "scripts": {
    "build:css": "tailwindcss -i ./views/input.css -o ./views/styles.css --minify",
    "watch:css": "tailwindcss -i ./views/input.css -o ./views/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
